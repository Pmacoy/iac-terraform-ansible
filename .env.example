# scripts/generate-ssh-key.sh writes the real .env (gitignored) for you.
# This file just documents the one variable docker-compose.yml expects.
BOOTSTRAP_SSH_PUBKEY=ssh-ed25519 AAAA... generated-by-scripts/generate-ssh-key.sh
